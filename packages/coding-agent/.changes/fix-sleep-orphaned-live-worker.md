- Fixed recovery treating a worker `list` or IPC timeout as proof of death. A live or identity-unknown worker now keeps its descriptor, kernels, tools, and transcripts untouched and is re-probed at a capped cadence until a complete roster restores routing; only a fresh `gone` or `replaced` identity may authorize uncertain-operation cleanup or relaunch.
